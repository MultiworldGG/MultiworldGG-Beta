# DEPRECATED: this workflow is kept only as a placeholder so the filename
# doesn't get reused by accident. Use `build-release-test.yml` (manual dry-run
# builds) or `release.yml` (tag-triggered releases) instead -- both build the
# same executable set via build_exe.py and stay current with FROZEN_TARGETS.

name: Build (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Fail with pointer to the current workflows
        run: |
          echo "::error::build.yml is deprecated. Use build-release-test.yml (manual dry-run) or release.yml (tag push) instead."
          exit 1
