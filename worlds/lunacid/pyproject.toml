[build-system]
requires = [ "setuptools",]
build-backend = "setuptools.build_meta"

[project]
name = "worlds.lunacid"
version = "0.0.1"
description = "MultiWorld: Lunacid"
classifiers = [ "Private :: Do Not Upload",]
requires-python = ">=3.12"
[[project.authors]]
name = "Albrekka"

[tool.setuptools.packages.find]
where = [ "src",]
include = [ "worlds.lunacid",]
namespaces = true
