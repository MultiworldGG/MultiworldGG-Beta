[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "worlds.smz3"
dynamic = ["version"]
description = "MultiWorld: SMZ3"
authors = [
    {name = "lordlou"}
]
license = "MIT"
license-files = [
    "LICENSE"
]
classifiers = [
    "Private :: Do Not Upload"
]
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src/"]
include = ["worlds.smz3"]
namespaces = true

[tool.setuptools.dynamic]
version = {attr = "worlds.smz3.Constants.VERSION"}
