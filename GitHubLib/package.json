{
  "name": "mwgg-github-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Oliver-Multiworld-Squirrel: GitHub App webhook receiver that publishes per-world releases into MultiworldGG-Index",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "probot": "^13.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.4",
    "vitest": "^3.1.4"
  },
  "engines": {
    "node": ">=22"
  }
}
